import 'package:flutter/material.dart';

class ApbarPage extends StatelessWidget {
  const ApbarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
